[package]
name = "vault_demo"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "vault_demo"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
idl-build = ["anchor-lang/idl-build", "rambu/idl-build"]

[dependencies]
anchor-lang = "1.2.0"
rambu = { path = "../rambu", features = ["cpi"] }
